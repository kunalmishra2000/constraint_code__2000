//5.Write a constraint to generate below pattern 9 19 29 39 49 59 69 79

class packet;
  rand int a[10];
  
  constraint x{  foreach(a[i])
    a[i]==(i*10)+9;
    
              }
  
  function void post_randomize();
    $display("a=%0p", a);
  endfunction
  
endclass

module tab;
  packet pac[];
  initial begin
    pac=new[4];
    foreach(pac[i])
      begin
        pac[i]=new();
        pac[i].randomize();
      end
  end
    endmodule 
