//3. Write a constraint to generate below pattern 0101010101...

class packet;
  rand bit[8:0] a;
  
  constraint x{  foreach(a[i])
    a[i]== (i%2);  //method 1
               //method 2
//     if(i%2==0)
//       a[i]==0;
//       else
//         a[i]==1;
              }
  
  function void post_randomize();
    $display("a=%b", a);
  endfunction
  
endclass

module tab;
  packet pac[];
  initial begin
    pac=new[4];
    foreach(pac[i])
      begin
        pac[i]=new();
        pac[i].randomize();
      end
  end
    endmodule 
